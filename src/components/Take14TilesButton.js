import React from 'react';

const Take14TilesButton = (props) => {

  return(
    <button className="take14tiles-button" onClick={props.handleTake14TilesClick}>Take Tiles</button>
  );

};

export default Take14TilesButton;
